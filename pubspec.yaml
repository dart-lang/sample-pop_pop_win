name: pop_pop_win
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  stagexl: ^2.3.1
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.2.1
  build_web_compilers: ^4.1.6
  checks: ^0.3.1
  dart_flutter_team_lints: ^3.0.0
  preload: ^2.0.0
  test: ^1.21.6
